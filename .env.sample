# Option 1: Using SA user (simplest for local development)
# This user has full permissions to create database, run migrations, and execute the app
MSSQL="SERVER=localhost;DATABASE=dynamic-schema-ef;UID=sa;PWD=P@ssw0rd!;TrustServerCertificate=True"

# Option 2: Using a dedicated application user (recommended for production-like setup)
# First run Database/00-create.sql with SA user to create database and user, then use:
# MSSQL="SERVER=localhost;DATABASE=dynamic-schema-ef;UID=dynamic-schema-test-user;PWD=Super_Str0ng*P@ZZword!;TrustServerCertificate=True"

# Azure SQL Database connection string
# MSSQL="SERVER=<your-server>.database.windows.net;DATABASE=dynamic-schema-ef;UID=<your-user>;PWD=<your-password>;TrustServerCertificate=True"
